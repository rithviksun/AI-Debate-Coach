[project]
name = "ai-debate-coach"
version = "0.1.0"
description = "An interactive debate coaching app."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.59.2",
    "openai>=1.0.0",
]

[tool.uv]
package = false
